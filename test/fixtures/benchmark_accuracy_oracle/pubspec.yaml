name: benchmark_accuracy_oracle
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  ffi:
    path: dependencies/ffi
  flutter:
    path: dependencies/flutter
  sibling:
    path: dependencies/sibling
  workmanager:
    path: dependencies/workmanager
